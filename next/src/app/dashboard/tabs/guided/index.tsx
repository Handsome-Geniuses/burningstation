import { cn } from "@/lib/utils"
import { useStoreContext } from "../../store"
import { MeterState } from "../../store/system"
import React from "react"



export const GuidedTab = () => {
    const { systemState } = useStoreContext()

    React.useEffect(()=>{

    })

    return (
        <div>
            hello world
        </div>
    )
}    
